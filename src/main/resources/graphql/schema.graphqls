type Query {
    authors: [Author]
    authorById(id: ID!): Author
} 

type Author{
    id: ID!
    name: String!
    books: [Book]
}

type Book {
    id: ID!
    title: String!
    publisher: String
}